@model IEnumerable<Travel_Website.Models.TinhThanh>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MaTinhThanh)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TenTinhThanh)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MoTa)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MaTinhThanh)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TenTinhThanh)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MoTa)
        </td>
        <td>
            @Html.ActionLink("Chỉnh sửa", "Edit", new { id=item.ID }) |
            @Html.ActionLink("Chi tiết", "Details", new { id=item.ID }) |
            @Html.ActionLink("Xóa", "Delete", new { id=item.ID }) |
            @Html.ActionLink("Xem hình ảnh" , "HinhAnhs", new {id = item.ID})
        </td>
    </tr>
}

</table>
